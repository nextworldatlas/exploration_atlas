# Postgres + PostGIS connection. The default is the dev cluster from `npm run db:init`
# (.dev/pgdata, port 5433). Any PostGIS 3.x database works; never use production here.
DATABASE_URL=postgres://postgres@localhost:5433/atlas
