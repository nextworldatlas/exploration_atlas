# Postgres + PostGIS connection. The dev scripts assume the portable cluster in .dev/
# (npm run db:start). Point this at any PostGIS 3.x database in other environments.
DATABASE_URL=postgres://postgres@localhost:5433/atlas
